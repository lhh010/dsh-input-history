//#region src/index.ts
/** Stable Cordis plugin name (matches the manifest id). */
const name = "@dsh-external/dsh-input-history";
/**
* Browser-only behavior; the host half is an empty registration shell.
* @param _ctx - host root context (unused).
*/
function apply(_ctx) {}
//#endregion
export { apply, name };
